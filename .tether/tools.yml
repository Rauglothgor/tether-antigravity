# tools.yml - Tool Discovery Registry (v2.0)

local_tools:
  - name: tether-cli
    description: Internal TETHER command line interface for state management.
    path: bin/tether.ps1

mcp_servers:
  # Registry for external Model Context Protocol servers
  # Use /mcp-discover to automatically search and add servers below
  - name: "Example: filesystem-explorer"
    path: "C:/path/to/server"
    description: "Sample registry entry for local MCP servers."
    enabled: false
