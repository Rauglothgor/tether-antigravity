# tools.yml - Tool Discovery Registry (v2.0)

local_tools:
  - name: gsd-cli
    description: Internal GSD command line interface for state management.
    path: bin/gsd.ps1

mcp_servers:
  # Registry for external Model Context Protocol servers
  # Use /mcp-discover to add new tools here
  - name: knowledge-graph
    description: Contextual relationship mapping for large codebases.
    enabled: false
